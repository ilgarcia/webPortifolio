import { Inter, Fira_Code } from "@next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "fallback",
  variable: '--inter-font',
});

export const firaCode = Fira_Code({
  subsets: ["latin"],
  display: "fallback",
  variable: '--fira-font',
});